{
  "name": "@pretorian-worx/runclaudia",
  "version": "0.19.0",
  "private": true,
  "description": "Diff-aware post-deploy test agent — v1 plan-only PR advisor",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "cli": "pnpm --filter @pretorian-worx/runclaudia-cli start"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.11",
    "@types/node": "^22.0.0"
  }
}
